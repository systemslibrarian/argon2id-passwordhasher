<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- Runnable walkthrough of the primary adoption path; one TFM is enough. -->
    <TargetFrameworks>net10.0</TargetFrameworks>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Argon2id.PasswordHasher.AspNetCore\Argon2id.PasswordHasher.AspNetCore.csproj" />
  </ItemGroup>

</Project>
